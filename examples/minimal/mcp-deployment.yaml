# yaml-language-server: $schema=../../schema/v1/kinds/mcp-deployment.schema.json
apiVersion: powerloom.app/v1
kind: MCPDeployment
metadata:
  name: team-files
  ou_path: /acme/engineering
spec:
  display_name: Team Files
  template_kind: files
  config:
    bucket_prefix: team-files
